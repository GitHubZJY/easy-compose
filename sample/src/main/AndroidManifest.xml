<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zjy.easy_compose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Easycompose">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Easycompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sample.swiperefresh.SwipeRefreshSample">

        </activity>

        <activity android:name=".sample.smsCodeInput.SmsCodeInputSample">

        </activity>

        <activity android:name=".sample.datePicker.DatePickerSample">

        </activity>
    </application>

</manifest>